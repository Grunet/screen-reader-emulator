[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[scripts]
clean = "invoke clean"
build = "invoke build"
start = "invoke start"
all = "invoke clean build start"

[dev-packages]
invoke = "*"
pyinstaller = "*"

[packages]

[requires]
python_version = "3.7"
